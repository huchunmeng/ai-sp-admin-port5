# 变更日志 — 影像报告书写训练（E2）

本文件记录「影像报告书写训练」模块的**文档与原型演进**，遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 的简化改编：日期段头 + `Added` / `Changed` / `Fixed` / `Removed` 四类，**不写版本号**（版本由 git 管理，遵守工作区「禁 `_v1` / `_v2` / `_最终版` 后缀」约定）。

## 来源说明

- 本模块的一日之内走完了 AI-PM 专家流程 Step 1–5，条目以**手工归纳为主**（依 PRD 三个过程稿的产出顺序与原型交付记录回溯），`git log` 仅作**交叉核对**，不作为条目来源。
- **`03_原型交付/` 与 `04_交付物/` 截至本文件生成时尚未提交 git**（工作区中为未跟踪/已修改状态），故最后一次「Step 5 同步校验」的改动无法用 commit 佐证，条目依据为 `差异对比清单_20260919.md` 的处置记录。
- 本文件**不记录** PRD 内部逐轮的行文修订（那属 PRD 自身演进）；只记录**产出物级别**的变化。

---

## 2026-09-19

模块从需求输入走到 Step 5 同步校验的完整一天。

### Step 1 · 需求结构化 → PRD 骨架版

**Added**

- 需求输入文档 `01_需求输入/需求_20260919_影像报告书写训练.md`：面向东南大学医学院的影像报告书写训练需求（六要素）。
- `02_PRD过程稿/PRD_骨架版.md`（40 KB）：需求六要素结构化后的 PRD 骨架，确立「训练 + 考核双入口」「R1 表 100 分 / 5 维度 / 23 条目」两大基线。

### Step 2 · 逻辑完备性审计 → PRD 补全版

**Added**

- `02_PRD过程稿/PRD_补全版.md`（110 KB）：逻辑完备性审计得分 **69 → 89 / 100**，补齐状态流转、异常边界、字段规则。

### Step 3 · PRD 质量审查 → PRD 定稿版

**Added**

- `02_PRD过程稿/PRD_定稿版.md`（126 KB）：PRD 质量审查将 🔴 严重缺失 **5 → 0**，去掉全部严重缺失项后定稿。

**Changed**

- 不可评机制成体系落地：§5.2.2 定义**甲类（去标识）/ 乙类（样本能力位 × 控件能力）** 两档，§5.10.2 给评分侧现算口径。
- §5.5.1 新增 `scoreableMax` 与**可评分下限 85**，并给出「为何取 85」的完整推导。
- 评分结果层改为 **raw / normalized 双存**（§5.5.1 ③），明确「影像底座补齐测量工具后历史成绩必须可重算」。
- §9.4 补测量工具的能力位与归属（归影像教学底座，口径 Q9）。
- 附录 E 扩为**不可评条目标注表**，逐条给出处置③。

### PRD 第四轮复核 · 进原型前

**Changed**

- 明确 R1 表 **4 条（16 分）本期不可评**的处置口径：`FIND-04` / `IMP-08` / `FIND-06` / `IMP-05` 四类同时落空 → `100 − 4 − 4 − 4 − 4 = 84 < 85` → **拒绝发布**。
- 甲类 `GEN-02` 定为**部分不可评**（检查号/影像号保留后 4 位仍可评），该条**整体仍计入分母**——这是 84 这个数不含甲类的原因。

**Added**

- `送审_待拍板清单_20260919.md`：**决策 5 / 提醒 3 / 问院方 10**。
- `问询单_待院方确认_20260919.md`：10 项待确认参数整理为可直接发出的文档。

### Step 4 · 原型生成

**Added**

- `03_原型交付/`：**8 页 / 19 文件**的高保真原型（4 CSS + 14 JS + 1 HTML）。
  - 页面：P1 模块首页（双入口）· P2 训练样本列表 · P3 训练工作台 T0–T4 · P4 对照自评页 · P5 我的考核任务 · P6 考核工作台 · P7 评分结果页 · P8 评审说明。
  - 形态：一屏单壳 + 侧栏 + 需求面板；`file://` 双击可用（不用 ESM import、不 fetch JSON，数据挂 `window.SEED`）；图标走内联 SVG（离线可用）；各页工厂在 try/catch 内执行，单页报错只死自己。
  - 视觉令牌逐条对齐 `apps/training/src/styles/variables.css`，顶栏版式仿 `TrainingLayout.vue`。
- **串讲编辑模式**：内容区与需求面板带虚线框的文案可直接改写，改动写入 `localStorage`（key = `hi-fi-prototype:report-writing:draft`），可导出 `prototype-config.json` + `prd-change-suggestions.md`；草稿超 30 天会询问是否续用。

### Step 5 · 同步校验

**Fixed**（原型侧，依 PRD 为权威；逐项见 `差异对比清单_20260919.md`）

- `caseLockReason` 枚举**去掉非法值 `switch`**，收敛为 `manual` / `timeout` 两值（C1）。
- `scoreableMax` 及算式**从学生可见区移入**标着「评审专用 · 学生端不展示」的「该例数据口径」卡（C2）。
- 金标准下发补上 `revealGoldStandardAfterSubmit` 条件：**默认 `false`**，考核侧默认不下发（C3）。
- 任务状态集**去掉多出的「已过期」**，收敛为 PRD 规定 8 态（C5）。
- `scorePolicy` 按 `first` / `highest` / `latest` 三值呈现（C8）。
- **整卷提交的 `lockReason` 传播修正**：未锁定的例统一按 `manual` 锁定、已锁定的例**保留原值**，整卷 `submitType` 另算为 `anyTimeout ? 'autoTimeout' : 'manual'`；弹窗文案与实现对齐（N3）。
- **路由命名统一**：`/report-writing/samples` → `/report-writing/train`、`/samples/:id` → `/report-writing/train/:caseId`（`app.js` / `p2-train-list.js` / `p8-notes.js` 三处）（N1）。
- P7 学生可见文案**删去 `rawTotal` / `normalized` 内部字段名与算式**，改为「按可评分项归一折算」，保留「4 条（16 分）落在能力边界之外」的透明说明（N11）。
- P3 一般信息条的全掩字段补「**本期不纳入评分**」标注（N12）。
- T4 补「**T0 阅片笔记回看**」卡，明示「不进入报告、不参与评分」（B1）。

**Added**（原型侧）

- P2 部位分组补「**其他**」、模态下拉补「**超声**」。
- P4 新增 T0 阅片笔记回看区；折算文案由硬编码改为**按样本派生**。
- P5 状态筛选补「**已交卷**」；banner 补两种计时模式（`whole` / `perCase`）与 `autoDeadline` 分流说明。
- P8 新增**第 5 能力位 `hasStagingInfo` 的「待回写 PRD」警示**。
- `app.js` 新增 `stage` 动作分支（阶段不可跳过的明确反馈）与 `input` 事件处理（段落实时字数 + 草稿保存时间）。
- P6 报告段 textarea 补 `maxlength`（与 `SEGMENTS[].limit` 一致）。

**Changed**（原型侧）

- P1 术语「L3 结构」→「**L3 要点**」，统一为「配额按「段 × 回合」发放，用完不补」。

**Added**（交付物，本目录）

- `blueprint.md`：12 节 + 封面元数据；第三节为 **8 行页面蓝图表**（与 `js/pages/` 下 8 个工厂文件一一对应），第十二节给出文件计数断言（19 文件 ≥ 8 页）。
- `data-specs.md`：按实体组织的字段规格（14 个 `window.SEED` 导出键），字段表统一列 `字段 / 类型 / 必填 / 取值枚举 / 说明 / 来源 §`；含**枚举字典**、**派生规则**、**三方对齐核查**（`b ⊆ a` 实测通过）。
- `差异对比清单_20260919.md`：Step 5 产物，10 列结构化清单；含**审计可信度锚点校验**（C1/C2/C3/C5 + A9 全部通过）。
- `PRD_影像报告书写训练.md`、`prototype/`：见下方「交付包归位」。

### 交付包归位（本目录）

**Added**

- `PRD_影像报告书写训练.md` ← 复制自 `02_PRD过程稿/PRD_定稿版.md`（**无版本后缀**，遵守工作区命名约定）。
- `prototype/` ← `03_原型交付/` 的**一次性复制快照**（结构完整保留）；`03_原型交付/` 本身不动，仍为上游工作目录。
- `自检报告_影像报告书写训练.md`：Step 6 自检报告，按法典 §6.3 模板。

### 第五轮 · 管理端承接补齐

**背景**：评审提出「训练的主操作页面是拟真的吗」「管理端需要有一个题库维护的模块不是吗」——第四轮把题库维护 / 组卷派发 / 成绩汇总归"教师端"并标"本期不做"，但 **port5 无教师端**，该归属悬空、被登记为风险 **R7**（考核功能不成立）。本轮按 `apps/admin` 承接口径补齐。

**Added**（PRD 侧，`PRD_定稿版.md`）

- **§5.12 管理端影像题库维护**：样本列表与筛选 · 影像序列入库（**zip 上传解包，只收 JPG / PNG 图片序列**，不收 DICOM）· 脱敏信息（姓名抹除 / 检查号·影像号保留后 4 位 / 检查日期保留年月；`GEN-02` 强制全掩）· 能力位声明 · 金标准报告录入 · **样本改版走新建版本、旧任务锁旧版**（§5.12.8）。
- **§5.13 管理端组卷与派发**：一页四步（基本信息 → 选样与权重 → 派发设置 → 确认发布）· 勾选范围 1–20 例 · 星级→权重内部归一 · **发布门禁二次确认**（§5.13.5）· 发布后不可改卷内病例与权重（§5.13.6）。
- **§5.14 管理端成绩汇总与学情**：三层下钻（任务级 → 学生级 → 病例级）· 均分只统计已交卷 attempt · 学情条目级失分率（样本量 < 5 不参与排序）· 申诉**只读登记**（本期不设复核 / 改分）· 导出写审计 `score.export`。
- **§6.1 页面清单拆学生侧 / 管理侧**，新增 **§6.5 管理端线框**。

**Changed**（PRD 侧）

- **第 5 能力位 `hasStagingInfo` 正式回写**（§5.2.2 / §5.10.2 / §5.12.5 / 附录 E / 附录 C V2）。4 位口径下 BDD 场景 30 的 84 分会算成 88 分，自相矛盾——原登记为待回写项（差异清单 A9），**本轮闭合**。
- `hasMeasurement` 归属澄清：**列在能力位里，但取值由影像控件能力决定**（§9.4 本期 `measurement: ❌` → 恒 `false`），入库侧**只读展示、不可勾选**。
- §3.1 / §4.1.1 归属表：原归"教师端"的四项改为 **管理端承接**；§1.2 不做项同步；**R7 闭合**（§十三）。
- §9.3 依赖表「教师端派发」改为「管理端派发 · 已闭合」。

**Added**（原型侧，`03_原型交付/`）

- **P9 影像报告题库**：8 列样本列表（含**能力位徽章**与**现算可评分**）· 样本改版影响面表 · `scoreableMax` 口径说明卡。
- **P10 样本编辑器**：四区块（影像序列入库 / 脱敏信息 / 能力位声明 / 金标准报告）+ sticky 页脚；已发布样本顶部**强制新建版本**提示。
- **P11 考核任务管理**：9 列任务表（含 `overrideGate` 折叠审计详情）· 「发布之后能做什么 / 不能做什么」表。
- **P12 组卷与派发四步**：一页四步 · 样本墙（勾选 + 5 星权重）· **整卷可评分三色带（含 85 刻度）** · 门禁两态与强制二次确认弹窗（必填原因 ≤ 200 字 + 写审计）· 两个选样预设（一键切「可发布 / 触发门禁」）。
- **P13 成绩汇总与学情**：三层下钻 · **不可评条目甲类 / 乙类分色**（灰 = 去标识，蓝 = 能力位落空）· 折算明细标「评审专用 · 学生端不展示」· 学情失分率排行 · 申诉只读登记。
- `css/admin.css`：管理端外壳与专属组件（步骤条 / 可评分三色带 / 样本卡 / 能力位徽章 / 上传区 / sticky 页脚）。
- `css/tokens.css` 追加 `--adm-*` 令牌块（对齐 `apps/admin/src/styles/variables.css`）。
- `js/icons.js` 追加 8 个管理端图标（upload / folder / sliders / users / ban / star / layers / history）。
- `js/seed.js` 追加管理端种子（`ADM_SAMPLES` / `SAMPLE_VER_IMPACT` / `ADM_TASKS` / `ADM_CLASSES` / `ADM_DRAFT` / `ADM_PRESETS` / `ADM_STUDENTS` / `ADM_TASK_STATS` / `ADM_ITEM_STATS` + 4 个派生函数）。
- `js/req-panel.js` 追加 5 个管理端需求键（逐条摘录 §5.12–§5.14）。
- P1 新增「**管理端承接的题库与考核管理**」卡（4 张入口小卡 → P9 / P11 / P12 / P13）。

**Changed**（原型侧）

- 侧栏分三组：`P1–P7 训练端 · 学生侧` / `P9–P13 管理端 · 承接 §5.12–5.14` / `— 说明`（P8）；**13 页**（原 8 页）。
- `PAGES[].end` 标记管理端页 → 渲染时给 `<body>` 加 `end-admin`，切到管理端令牌与观感（深蓝顶栏 + `#1890FF` 主色）；`#protoBadge` 同步显示「训练端 / 管理端 · 高保真原型」。
- 门禁落点从「教师端组卷」改为「**管理端组卷（P12 第 4 步 / §5.13.5）**」，计量单位明确为**整卷加权**——原登记的口径分歧（差异清单 N9）**闭合**。
- 全文「教师端」改为「管理端」（保留讲述历史归属的必要叙述）。
- P8 交付范围表新增 5 行管理端页；形态与视觉说明改为**两端令牌**；`hasStagingInfo` 的「待回写 PRD」⚠️ 改为 ✅「已回写」。

**Fixed**（原型侧）

- **门禁审计原因恒为空串**：`util.js` 的 `closeModal()` 会**先清空弹层 DOM、再回调 `onClose`**，原实现于 `onClose` 内 `getElementById('_gateReason')` 取值 → 恒为 `''`，`exam.publish.overrideGate` 的 `reason` 丢失。改为在 `validate()`（DOM 尚在）时取走并缓存。
- `p11-adm-exam-tasks.js` 运算符优先级 bug：`'…' + t.assignee.type === 'class' ? … : …` 整段拼接被当作三元条件判断式（恒真），修复为 `'…' + (t.assignee.type === 'class' ? … : …)`。
- **`H.rich()` / `H.ed()` 参数误传 HTML 标签 → 标签被当字面文本渲染给用户**（**第六轮补修**，7 个文件）：两个 helper 内部都是 `esc(s)` 打底，传进去的 `<b>` / `<code>` / `<span class="mono">` 会被转义成 `&lt;b&gt;` 显示出来。命中 7 处：`p2-train-list.js`（继续上次条的 `.cs-title`）、`p4-self-review.js`（自评已提交时间戳）、`p8-notes.js` ×2（被移出分母的四类条目 / 门禁演示计数）、`p9-adm-samples.js`（样本改版影响面横幅）、`p12-adm-exam-create.js`（sticky 页脚可评分）、`p13-adm-scores.js`（页头副标题）、`app.js:290`（整卷提交弹窗）。修法分两类：能转 markdown 的改 `**x**` / `` `x` ``，需要带 class 的（`mono` / 内联色）把标签挪到 `rich()` 之外拼接。
  - **为什么前几轮没抓到**：Step 5 / 第五轮的验证用的是**关键词断言**（断言某串文字在不在 DOM 里）——被转义的标签**仍然是那串文字**，关键词照样命中，所以断言全绿而页面上明晃晃列着 `&lt;b&gt;`。改为**渲染后扫描字面转义标签**才暴露。
  - **附带产出**：新增静态检测脚本（扫 `H.rich(` / `H.ed(` 实参里的字符串字面量是否含 `<tag>`），比渲染扫描更可靠——条件分支（如 P4 未提交态）不渲染时也能查到。

**Changed**（交付物，本目录）

- `blueprint.md`：页面表 **8 → 13 行**；新增 P9–P13 行；文件计数断言改为 **25 文件 ≥ 13 页**；新增双端视觉基线对照表；角色分工表改为管理端承接。
- `data-specs.md`：实体总览 **14 → 28 键**；新增第十二节「管理端实体（题库 / 组卷 / 成绩）」共 12 小节；§4.3 `hasStagingInfo` 标 ✅ 闭合、新增 §4.4 `hasMeasurement` 归属澄清；枚举字典与派生规则补管理端项；三方对齐核查实测 **`b ⊆ a` 24 / 24 通过**。
- `prototype/` 重新快照：**19 → 25 文件**（5 CSS + 19 JS + 1 HTML）。
- `PRD_影像报告书写训练.md` 重新同步（与定稿版 diff 为零）。

### 第六轮 · 交付后复核（渲染转义补修）

**背景**：交付完成后复核「原型真的能跑吗」，把验证方式从**关键词断言**换成**渲染后扫描字面转义标签**，抓出一类前三轮都漏掉的缺陷。

**Fixed**（原型侧，7 文件）

- 见上方「第五轮 · Fixed」第三条：`H.rich()` / `H.ed()` 参数误传 HTML 标签，7 处命中，全部修掉。

**Added**（原型侧）

- 无新增页面或文件——本轮**只修不增**，原型仍为 **13 页 / 25 文件**。

**Changed**（交付物，本目录）

- `prototype/` 按修复后的 `03_原型交付/` 重新快照（25 文件不变，内容更新）。
- `差异对比清单_20260919.md` 追加 **5.4 节新发现 F3**。
- `自检报告_影像报告书写训练.md`：⚠️5 由 2 项扩为 **3 项**；新增「验证方式本身的缺口」说明；取证命令改为 **13 页 + 字面标签扫描**。

---

## 未纳入本文件的记录

- **PRD 内部逐轮行文修订**：属 PRD 自身演进，`git log` 中查 `PRD_定稿版.md` 的提交历史。
- **`apps/training/src/data/reportCases.js` 的既有示例样本**：原型种子的数据来源之一（见 `03_原型交付/js/seed.js` 头注），但其变更属训练端仓库，不在本模块范围。
- **`apps/admin/` 的既有模块**：本轮**零触碰**——管理端承接只体现在 PRD 与原型，尚未落到管理端代码。
- **影像教学底座**：调窗 / 测量 / MPR / 序列管理归底座（Q9），本模块只消费其能力位。
