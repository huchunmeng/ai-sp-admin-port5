# 批注响应 · 第六轮（2026-09-20 评审 3 条）

> 响应日期：2026-09-20 ｜ 依据：第五轮交付后老胡的 3 条批注（均在工作台）
> **3 条全部落地并实测**。核心是把「影像显示控件」与「患者信息」**合成一块双 tab 面板**，并让 AI伴学高度自适应屏幕。

---

## 一、逐条处置

| # | 批注原话 | 处置 | 落在哪个文件 |
|---|---|---|---|
| 1 | 「影像显示控件 MR · 1 个序列 / 共 1 帧 滚轮 / ↑↓ 翻层面 → **影像显示空间改为影像图片，与患者信息做成两个 tab**」 | 卡片头从「影像显示控件 + 标签」改为**两个 tab 按钮**：`影像图片` / `患者信息`。卡片标题即 tab；序列信息（`DR · 1 个序列 / 共 1 帧`）与翻层提示（`滚轮 / ↑↓ 翻层面`）移到 tab 行右侧，**只在「影像图片」tab 下显示** | 新增 `components/ImagePanel.vue` |
| 2 | 「文本：患者信息 → **改为跟影像图片放到一起作为一个 tab 使用**」 | 第五轮做的**左侧浮动面板整个撤掉**（圆形 ⓘ 按钮 + 390px 浮层删除），患者信息改为**同一张卡片的第二个 tab**，字段与"复制到报告"交互一字未改 | 新增 `components/PatientInfoTab.vue`；删除 `components/FloatPatientInfo.vue` |
| 3 | 「AI伴学 0 问 → **整个板块的高度自适应屏幕高度**」 | 面板高度改为 `calc(100vh - 82px)`（顶部站点条 58px + 上下留白），**聊天区自己滚动、快捷问题与输入框钉在底部**；删掉原来写死的 `max-height: 420px` | `components/CompanionPanel.vue` |

**为什么用 tab 而不是并排**：一块屏同时放"大图 + 9 个患者字段 + 报告输入 + AI伴学"必然有人被挤到看不见。影像与患者信息是**交替看**的两件事（看图时不需要患者字段，抄患者信息时不需要图），做成 tab 才让每一件都有完整空间。

**顺带修掉一处**：`影像待接入` 这句状态此前**无条件显示**——SEU 系列有真实影像的病例底下也挂着这句话。现已改为**只有本病例一张真实图都没有时才显示**。

## 二、结构变化

```
改前                                       改后
┌ 影像显示控件 DR · 1 个序列 / 共 1 帧 ┐   ┌ [影像图片] [患者信息]  DR · 1 个序列… 滚轮/↑↓ ┐
│  序列条 / 画布 / 翻页              │   │  序列条 / 画布 / 翻页（tab 1）            │
│  影像待接入                        │   │  或  患者信息字段 + 复制（tab 2）         │
└────────────────────────────────────┘   └──────────────────────────────────────────┘
+ 左上角 ⓘ 浮动患者信息（第五轮）           （浮动面板撤掉）
```

| 文件 | 变化 |
|---|---|
| `components/ImagePanel.vue` | **新增**：卡片外壳 + tab 头（`影像图片` / `患者信息`）+ 头部元信息；tab 状态是本组件局部状态 |
| `components/PatientInfoTab.vue` | **新增**：患者信息字段网格（3 列）+ 临床主要信息及检查目的（整段复制）；由 `FloatPatientInfo` 的浮层内容抽出 |
| `components/ImageViewer.vue` | 改为**纯图片主体**：去掉自带的 `.card` 外壳与卡片头（改由 ImagePanel 提供），根节点变 `.rwb-viewer-root`；`影像待接入` 改为按"有无真实影像"条件显示 |
| `components/CompanionPanel.vue` | 面板高度自适应屏幕；聊天区去掉写死 max-height |
| `ReportWritingWorkbench.vue` | `<ImageViewer>` + `<FloatPatientInfo>` → `<ImagePanel :sample @copy>` |
| `components/FloatPatientInfo.vue` | **删除**（第五轮刚做，本轮按批注改回 tab） |

## 三、实测（Playwright，chromium，1600×950，SEU-002 真实影像）

| 检查项 | 结果 |
|---|---|
| tab 列表 | `["影像图片","患者信息"]` ✅ |
| 默认 tab | `影像图片` ✅ |
| 卡片头文案 | `影像图片 \| 患者信息 \| DR · 1 个序列 / 共 1 帧 \| 滚轮 / ↑↓ 翻层面` ✅ |
| 浮动面板残留 | `.float-info-trigger` **0 个** ✅ |
| 真实影像 | `naturalWidth×naturalHeight = 411×235`，加载成功 ✅ |
| `影像待接入` 误显示 | **false**（SEU-002 有真实影像，不再挂这句） ✅ |
| 切到患者信息 | 激活态变 `患者信息`，字段顺序 `患者姓名 患* / 年龄 70–79 岁 / 性别 男 / 科别 呼吸内科 …` ✅ |
| 切回影像图片 | 图仍在（`naturalWidth=411`） ✅ |
| AI伴学高度自适应 | 窗口 950px → 面板 **868px**（=950−82）；700px → **618px**；1200px → **1118px** ✅ 三档都对得上 |
| 输入框位置 | 面板底 928px（视口 950px），输入框钉在面板底部；聊天区 624px 且可滚动 ✅ |
| 构建 | `build:training` ✅ |
| console | **零错误** ✅ |

## 四、遗留

| # | 事项 | 状态 |
|---|---|---|
| 1 | 单序列病例的图仍是 `max-width: 400px` 居中限宽（第四轮为避免把报告输入挤出首屏定的） | 若嫌小可放大到 520–600px，等老胡定 |
| 2 | RC-001/002/003 的影像仍是斑马纹占位 | 待老胡定：是否用院方真实影像替换 |
| 3 | 训练记录只存本机（localStorage），无服务端 | 接口就绪后只换 `useReportSession` 的读写实现 |
| 4 | 院方样例标准报告由 AI 起草、未过医师复核；像素级脱敏未做 | 已在 `samples-seu.js` 头部与 `sampleNote` 声明 |
| 5 | 红线规则 B1 未实现；红线校验在前端 | 已登记 |
| 6 | 考核管理（P11–P13）零代码 | 按既定范围不动 |
