# 验收截图 · 批注响应（2026-09-22）

> 来源：评审批注汇总报告（2026/9/22 15:31），两条都在 `reportWritingExam`（考试室）。

## 1. 批注 1 ——「整行放到顶部栏去」

> 批注原文：`剩余 29:16 | 1 / 1 · 胸部 · CT | 练习考（不计成绩） | 交卷` → 整行放到顶部栏去

**改法**：考试状态不再单独占一行（`.ex-bar` 已删除），整行并入页面自带的 `TrainingTopBar`：

| 原状态栏元素 | 并入位置 |
|---|---|
| `剩余 29:16` | 顶部栏**自带的计时器**（`formatted-time` + `timer-class`：≤10 分钟黄、≤5 分钟红） |
| `1 / 1 · 胸部 · CT`、`练习考（不计成绩）`、`已自动保存`、`离开记录 N 次` | 顶部栏的 **center 插槽** |
| `交卷` | 顶部栏的**结束按钮**（`end-label="交卷"`、`@end="askSubmit"`） |

实测（进入练习考后读顶部栏文本）：

```
"练习考进行中 1 / 1 · 胸部 · CT 练习考（不计成绩） 已自动保存 剩余 29:58 交卷"
.ex-bar 行数: 0   顶部栏计时器: 1   结束按钮: 1   center 插槽: 1
```

## 2. 批注 2 ——「测量无法点击，无法使用」

> 批注原文：（锚在「缩放」按钮上）测量无法点击，无法使用

**根因**：`canMeasure` 原先要求「该序列有 `PixelSpacing`」，而胸部 CT 是 **JPEG 序列**（没有 `raw`/间距）
→ 按钮被**禁用**。不是坏了，是被设计成"没有间距就不给测"。

**改法**：**没有像素间距时按像素测量，不再禁用**；有间距时仍然是毫米（口径不变）。

| 场景 | 改动 |
|---|---|
| 判定 | `canMeasure` = 有画面即可测（`hasRaw \|\| currentImage`），只有 16-bit 解码不可用（O5 降级）才禁用 |
| 坐标换算 | 新增 `imgEl` + `imgNatural` + `viewSize`：JPEG 用 `<img>` 的自然尺寸换算，不再假设只有 canvas |
| 单位 | `measureUnit`：有间距 → `mm`，无间距 → `px`；标注与底部结果条都带单位 |
| 提示 | `measureTitle` 分三种：正常毫米 / 无间距按像素 / 16-bit 解码不可用 |
| 数据字段 | 测量对象由 `{mm}` 改为 `{len, unit}`（避免字段名叫 mm 实际是 px） |

实测：

```
JPEG 序列（PUB-001，<img> 渲染）：按钮 disabled=false · 拖出 "18 px"
16-bit 序列（KNEE-001，canvas）  ：按钮 disabled=false · 拖出 "126.1 mm"  ← 毫米口径未受影响
考试室（练习考，胸部 CT）        ：按钮 disabled=false · 拖出 "18 px"
```

## 3. 说明

- 三条 build（admin / training / exam）均通过；上述流程 `pageerror` 为 0。
- 本轮**未改**评分算法、红线、计时与交卷逻辑；`ImageViewer` 是两端共用组件，所以训练工作台与考试室同时生效。
- 考试端考站机（`apps/exam`）的状态条未动 —— 那是考站机自己的外壳，本次批注未涉及。
