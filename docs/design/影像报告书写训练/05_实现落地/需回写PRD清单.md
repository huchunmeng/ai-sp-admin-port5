# 需回写 PRD / 原型 / data-specs 清单

> 用途：**代码已按评审批注改完，但设计交付物（PRD 定稿版 / 13 页原型 / data-specs）还是旧口径**。
> 老胡的决定是「PRD 和原型可以全部改完后再改」，故集中登记在此，改的时候对着这一页走。
> 生成日期：2026-09-19 ｜ 依据：两轮批注（`批注响应_20260919_训练工作台.md`、`批注响应_20260919_第二轮.md`）

**共 6 组变更、28 处落点。** 前 4 组来自第一轮，后 2 组来自第二轮。

---

## 变更 1 · 训练流程：五阶段 → 两态

| 项 | 原 | 现 |
|---|---|---|
| 流程 | **五阶段** T0 阅片 → T1 检查技术 → T2 影像所见 → T3 诊断意见 → T4 对照自评，逐段推进 | **两态**：书写报告（三段同时可写）→ 对照参考 |
| 推进门槛 | 空段不可推进阶段 | **提交时三段必须非空** |

| # | 文件 | 位置 | 要改什么 |
|---|---|---|---|
| 1.1 | PRD | §5.2（含 5.2.1–5.2.5） | 五阶段流程整节重写为两态；`stage` 概念去掉 |
| 1.2 | PRD | §5.4.1 规则 2 | "训练侧空段不可推进阶段" → "训练侧提交时三段皆非空" |
| 1.3 | PRD | §5.10.2 数据模型 | `round{n}.stage` / `draft.stage` 字段去掉 |
| 1.4 | PRD | §6.2 训练工作台线框 | 阶段条画成两态；区块顺序按变更 5 重画 |
| 1.5 | PRD | §7.1 训练侧状态流转 | 回合级状态图按两态重画 |
| 1.6 | data-specs | §7.2 `WORKBENCH` | `stage` 由 `T0…T4` 改为 `write` / `review` |
| 1.7 | 原型 | P3 训练工作台 | 阶段条、逐段推进逻辑、无阶段条时的段高亮 |

---

## 变更 2 · 取消逐条自评（第二轮批注 1）

| 项 | 原 | 现 |
|---|---|---|
| T4 | 逐条自评 R1 表 23 条（写了/没写/不确定），自评合计 + 与系统分偏差 | **整块取消** |
| 看对照的门禁 | 自评未提交不渲染对照（`409 SELF_REVIEW_REQUIRED`） | **取消**——提交报告即可对照 |
| 训练终态 | 完成一例 = 提交 T4 自评 | **完成一例 = 提交报告** |
| 卡片字段 | 已练 N 次 + **最近自评得分** | 已练 N 次 + **最近练习时间** |

| # | 文件 | 位置 | 要改什么 |
|---|---|---|---|
| 2.1 | PRD | §5.2.4（提交后的自评环节） | **整节作废**，改为说明"为什么不做自评"（评审批注：自评没什么必要） |
| 2.2 | PRD | §5.8 报告对照 | 删掉"自评先行 / 门禁 / `409 SELF_REVIEW_REQUIRED`"；保留左你的报告 / 右参考报告双栏 |
| 2.3 | PRD | §5.2.5 重写与重练 | "完成一例 = 提交 T4 自评" → "完成一例 = 提交报告"；自评幂等契约整段删 |
| 2.4 | PRD | §5.3 训练样本列表 | 卡片信息去掉"最近自评得分" |
| 2.5 | PRD | §6.4 评分结果页线框 | 凡涉及"自评对比"的部分删（若该页仅属考核侧，则只删训练侧引用） |
| 2.6 | PRD | §十二 BDD 场景 | 涉及自评的验收场景删除或改写 |
| 2.7 | data-specs | §二 实体总览 | 删 `SELF_REVIEW` 行 |
| 2.8 | data-specs | §十一 `SELF_REVIEW` | **整节删除** |
| 2.9 | data-specs | §14.1 枚举字典 | 删「自评判定 `marks` 值」一行 |
| 2.10 | data-specs | §14.2 派生规则 | 删 `SELF_REVIEW.deviation` 一行 |
| 2.11 | 原型 | P4 对照自评页 | 自评表部分删除，页面退化为"对照参考"页（或与 P3 对照态合并） |

---

## 变更 3 · 取消「影像所见 · 要素自检」（第二轮批注 3）

| 项 | 原 | 现 |
|---|---|---|
| 面板 | 训练工作台右栏的要素覆盖清单（●已覆盖 / ?存疑 / ○缺失） | **整块删除**（含其前端启发式判读实现） |

| # | 文件 | 位置 | 要改什么 |
|---|---|---|---|
| 3.1 | PRD | §6.2 线框右栏 | 删掉 `●已覆盖 / ○缺失 / ?存疑` 三行 |
| 3.2 | PRD | §5.8 | 删"`coverage` 仅训练侧下发、考核期不下发"的口径 |
| 3.3 | data-specs | §7.2 `WORKBENCH` | 删 `coverage[]` 及其三个子字段 |
| 3.4 | data-specs | §14.1 | 删「覆盖标记 `coverage[].mark`」一行 |
| 3.5 | 原型 | P3 训练工作台 | 删右侧覆盖清单面板 |
| 3.6 | — | 已删代码 | `apps/training/src/views/report-writing/coverage.js`、`COVERAGE_ELEMENTS` |

---

## 变更 4 · 培训提示 → AI伴学，数据源改为大模型读金标准（第二轮批注 4）

| 项 | 原 | 现 |
|---|---|---|
| 命名 | 培训提示 / 提示栏 | **AI伴学** |
| 数据源 | "提示模板 + 模型输出"（未定实现）；本期先做静态库 | **大模型读题目答案（金标准报告）+ 学员该段已写内容**，产出少量提示；L1 体裁提示走静态库（不调模型） |
| 红线校验位置 | 服务端出站层 | 本期前端兜底（正式版仍应回服务端） |

| # | 文件 | 位置 | 要改什么 |
|---|---|---|---|
| 4.1 | PRD | §5.7 提示引擎 | 改名 AI伴学；明确数据源与 L1 静态 / L2·L3 模型的分工；补"本期红线校验在前端兜底"的过渡说明 |
| 4.2 | PRD | §5.2.3 三级提示阶梯 | 术语随改名调整（"提示"→"伴学提示"），配额与冷却规则**不变** |
| 4.3 | PRD | §9.5 红线 | 补登"B1 结论短语自动抽取本期未实现，靠红线 A 兜底；改写措辞复述结论为已知缺口" |
| 4.4 | 原型 | P3 训练工作台右栏 | 面板改名 AI伴学，样式与既有 AI伴学（`AICompanionDrawer`）统一 |

---

## 变更 5 · 阅片笔记的位置（两轮批注合并）

批注原话：第一轮「是否应该放到影像显示空间下面更合理？」→ 第二轮「放到最后吧」。

**最终位置**：主列**最末**。区块顺序固定为：

```
一般信息（可收起展开）
影像显示控件（序列数量随病例变）
学生报告（三段同时可写；对照态下收起，由对照区左栏呈现）
对照参考（仅对照态）
阅片笔记（默认收起，可选、不参与评分）
```

| # | 文件 | 位置 | 要改什么 |
|---|---|---|---|
| 5.1 | PRD | §6.2 线框 | 按上面顺序重画 |
| 5.2 | PRD | §5.2.2（T0 阅片笔记） | "T0 阶段"的表述去掉，改为"影像区下方的可选草稿区，不进入报告、不评分" |
| 5.3 | data-specs | §7.2 `WORKBENCH.viewNotes` | 说明去掉"T0"字样 |
| 5.4 | 原型 | P3 | 笔记挪到最末、默认收起、加用途说明 |

---

## 变更 6 · 影像序列：三视图 → 按样本声明的有序列表

| 项 | 原 | 现 |
|---|---|---|
| 形状 | `series: { axial, coronal, sagittal }` | `series: [{ key, name, en, frames }]`，**数量随样本**（1–5 个） |
| 三视图 | 硬性约定 | 降为**新建样本的默认模板**，可增/删/改名/调顺序，支持自定义视图名 |
| 层面浏览交互 | 未定形态（原型用拖动条） | **图片切换**：滚轮 / ↑↓ / 上下张 / 层号输入；拖动条取消 |

| # | 文件 | 位置 | 要改什么 |
|---|---|---|---|
| 6.1 | PRD | §5.12.3 影像序列入库 | 三视图分区上传 → 序列列表，可增删改排序；上传校验（≤300 张 / ≤5MB / jpg·png）不变 |
| 6.2 | PRD | §5.12.7 `imagingSample` | `series` 结构改为有序列表 |
| 6.3 | PRD | §9.4 影像显示控件接口契约 | 补：视图数量可变；补层面浏览的交互形态（图片切换，非拖动条） |
| 6.4 | data-specs | §12.3 `ADM_SAMPLES.series` | 同上改为有序列表 |
| 6.5 | data-specs | §14.1 枚举字典 | 若列了三视图键，改为"视图集合随样本声明" |
| 6.6 | 原型 | P3 / P10 | 三视图 → 序列条 + 自适应；拖动条 → 图片切换 |

---

## 附：两轮批注没有改的口径（回写时别顺手改掉）

1. **提示配额**：L1 不限 / L2 每段每回合 3 次 / L3 每段每回合 1 次；同级冷却 10 秒；**重写不重置、新回合才重置**
2. **配额「受理即原子预扣 + 失败回滚」**（PRD §5.7.2）——实现已按此做，并有实测
3. **脱敏口径与不可评归一折算**（脱敏形态即评分基准；全掩字段分值自分母剔除、不记 0 分；可评分 < 85 拒绝发布）
4. **R1 表**（100 分 / 5 维度 / 23 条目）与 `scoreableMax` **现算**、`rawTotal` / `total` **双存**
5. **考核侧由管理端承接**（组卷与派发 / 成绩汇总），本期未实现——**这条也没变**
6. **影像控件进阶能力**（调窗 / 测量 / MPR）归影像教学底座，本期不做

## 附：本轮之前已登记、**未**随批注变化的交付物偏差

- `data-specs.md` §六表的 `RC-005` / `RC-008` 可评分写作 **84**，按同文件口径现算应为 **88**（代码按 88 实现）
