# 批注响应 · 第十四轮（2026-09-20，3 条 · 训练工作台）

| # | 批注原话 | 处置 |
|---|---|---|
| 1 | 「按钮：患者信息 → 与下面的患者临床信息重复，删掉」 | ✅ **删掉「患者信息」tab**：患者信息已并入报告第一段「患者临床信息」（段一），面板不再需要第二个 tab。`PatientInfoTab.vue` 整文件删除，`ImagePanel` 去掉 tab 机制与 `@copy` 事件，标题改为固定「影像图片」 |
| 2 | 「患者姓名：患*，年龄：50–59 岁，性别：女，科别：神经内科，检查时间：2026-03-15 09… → 调整好格式，方便阅读和查看，符合真实场景格式要求」 | ✅ **按真实申请单排版**：一般项目做成三列标签-值网格（姓名/年龄/性别、科别/检查时间、检查部位/检查方法），病史与检查目的各自**另起一段**并带小标题，段间虚线分隔 |
| 3 | 「颅脑MRI MRI 上一张 / 1 下一张 → 这里按照PACS的要求做成真实的阅片工具结构」 | ✅ **改成 PACS 式阅片工具**：顶部工具条 + 左侧序列栏 + 影像区四角叠加信息 + 底部层面滑动条。见下 |

## 一、段一改成申请单式排版（批注 2）

```
患者临床信息                                          系统给出
─────────────────────────────────────────────────────────────
患者姓名 患*        年龄 60–69 岁      性别 男
科别 胸外科        检查时间 2026-08-18 14:40
检查部位 胸部       检查方法 CT
─────────────────────────────────────────────────────────────
患者病史
胸痛、消瘦3个月
检查目的
胸部CT平扫
```

- **一般项目 / 检查信息**：三列 `标签 值` 网格，值加粗，像申请单抬头
- **患者病史 / 检查目的**：各自成段带小标题（这两项本来就是叙述性文字，塞进一行读不了）
- 数据结构从「一段拼好的字符串」改为 `{ items, study, fields }`，由 `Workbench` 从样单元数据组装

## 二、PACS 式阅片工具（批注 3）

```
┌ 工具条 ─────────────────────────────────────────────────────────┐
│ 窗宽窗位 [肺窗轴位][纵隔窗][骨窗][脑窗] │ 层面 ⏮ ◀ ▶ ⏭ │ 测量 缩放 复位 │
├──────┬──────────────────────────────────────────────────────────┤
│ 序列2 │ 患*                    │            胸部 CT             │
│ ┌──┐ │ 男 · 60–69 岁          │            肺窗轴位            │
│ │▣ │ │ 胸外科                 │      [ 影像 ]                  │
│ └──┘ │                        │                                │
│ 肺窗  │ W: 1500 L: -600        │              层 1 / 75         │
│ 75帧  │ Lung                   │              缩放 100%         │
│ ┌──┐ ├──────────────────────────────────────────────────────────┤
│ │▣ │ │ 1 ──────●────────────────────────────────────────── 75   │
│ 纵隔  │
└──────┴──────────────────────────────────────────────────────────┘
```

| 结构 | 做法 |
|---|---|
| **工具条** | 窗宽窗位**预设**（序列自带窗 + 肺窗/纵隔窗/骨窗/脑窗，点击即换，当前项高亮）；层面 ⏮◀▶⏭；`测量`/`缩放` **置灰并注明「本期影像控件不具备」**（不假装能做） |
| **左侧序列栏** | 每个序列一张**中间层缩略图** + 序列名 + `英文名 · N 帧`，当前序列高亮左边条 |
| **影像区** | 纯黑底 + **四角叠加信息**：左上 患者姓名/性别年龄/科别，右上 检查部位·模态/序列名/病例标题，左下 `W: … L: …` + 英文序列名，右下 `层 x / N` + `缩放 100%` |
| **底部层面滑动条** | `1 ──●── 75`，可拖；滚轮 / ↑↓ / 点左半屏右半屏翻层照旧 |
| **复位** | 回到首帧 + 清除窗口预设 |

## 三、实测（Playwright）

| 检查项 | 结果 |
|---|---|
| 批注 1 | `.rwb-tab` **0 个**；面板标题 `["影像图片"]`；`PatientInfoTab.vue` 已删除、无残留引用 ✅ |
| 批注 2 | 一般项目 `患者姓名 患* / 年龄 60–69 岁 / 性别 男 / 科别 胸外科 / 检查时间 2026-08-18 14:40 / 检查部位 胸部 / 检查方法 CT`；成段 `患者病史 胸痛、消瘦3个月`、`检查目的 胸部CT平扫` ✅ |
| 批注 3 | 工具条 `窗宽窗位 / 肺窗轴位(active) / 纵隔窗 / 骨窗 / 脑窗 / 层面 / 测量 / 缩放 / 复位`；序列栏 `肺窗轴位 Lung · 75 帧`、`纵隔窗轴位 Mediastinum · 75 帧`；四角叠加 `患* 男 · 60–69 岁 胸外科` / `胸部 CT 肺窗轴位 CT·右肺上叶巨大肿块` / `W: 1500 L: -600 Lung` / `层 1 / 75 缩放 100%`；滑动条 `min=1 max=75` ✅ |
| 构建 / console | `build:training` 通过，**零 console 错误** ✅ |

## 四、遗留

| # | 事项 | 状态 |
|---|---|---|
| 1 | 窗宽窗位只换**标注**，没有真正重算像素 | 逐像素重算需要 canvas + 原始 HU，本期影像是导出的 JPEG；要做真窗宽窗位得换回 DICOM 或另存多套窗图 |
| 2 | `测量` / `缩放` 置灰 | 与评分表「测量工具不可评」的口径一致；将来接上控件时两处一起开 |
| 3 | 序列栏缩略图取中间层 | 真实 PACS 是首帧或定位像；要换成定位像需要额外素材 |
