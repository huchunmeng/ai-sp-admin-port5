# 影像报告书写训练 · 考核功能设计

> 日期：2026-09-20 ｜ 前置：`考核侧方案与工作量评估_20260920.md`（已定走 B 路，需服务端）
> 本文是**交互与页面设计**，供出原型与开发使用。工量与实施顺序见方案文档。

---

## 1. 设计目标与三条红线

**目标**：让"影像报告书写"这件事**可被正式考核** —— 成绩可信、可复核、可解释。

**三条红线**（贯穿全部页面，任何设计妥协都不能碰）：

| # | 红线 | 落地含义 |
|---|---|---|
| R1 | **不给答案** | 考试期间与交卷后，**都不下发标准报告、评分要点、逐要点点评**以外的信息；成绩报告**没有「报告对照」页签** |
| R2 | **时间可信** | 计时以**服务端**为准，前端倒计时只是显示；交卷由服务端校验 |
| R3 | **留痕可复核** | 答卷原文、评分结果、人工改动、申诉都留痕；成绩可追溯到具体一次作答 |

> 这三条决定了本文与训练侧的大量差异（§3）。

---

## 2. 角色与流程

| 角色 | 做什么 |
|---|---|
| **考务管理员** | 组卷 → 发布（定名单与考试窗口）→ 监控 → 处理异常 |
| **考生** | 登录 → 阅读须知 → 作答 → 交卷 |
| **阅卷/教师** | 查看成绩与明细 → 复核/改分 → 导出 |

```
                ┌── 组卷 ──┐
管理员 ──▶ 试卷（选题/规则/时长/达标线）──▶ 发布到班级或名单 + 考试窗口
                                    │
考生 ──▶ 登录校验 ──▶ 考试须知 ──▶ 开考（服务端签发 startedAt/deadline）
                                    │
                          ┌─────────┴─────────┐
                     下发题面（无金标准）   逐题作答 + 自动保存
                          └─────────┬─────────┘
                              交卷（手动 / 到点自动）
                                    │
                        答卷入库 ──▶ 异步批阅队列（服务端 LLM）
                                    │
              成绩入库 ──▶ 教师查看明细 ──▶ 复核/改分 ──▶ 导出
```

---

## 3. 与训练侧的差异（**设计的核心**）

| 维度 | 训练侧（现状，已实现） | 考核侧（本设计） | 为什么 |
|---|---|---|---|
| 标准报告 | 随题下发，可看「报告对照」 | **不下发**，交卷后也不给对照 | R1 |
| 评分时机 | 学员点提交后前端调 LLM | **交卷后服务端异步批阅** | 考试期可能断外网；并发要队列 |
| 计时 | 无 | **服务端计时**，到点自动交卷 | R2 |
| AI伴学 | 有 | **无** | 考核不提供辅助 |
| 阅片笔记 | 有（不评分） | 有（**不评分、不计入答卷**） | 允许草稿纸 |
| 题目顺序 | 固定 | 可**每人随机**（等价题组内） | 防抄袭 |
| 中途退出 | 无约束 | 记录离开次数；超次数可告警 | 威慑级 |
| 重练/修改 | 随时 | **不可修改**；重考需新开一次考试 | 成绩可信 |
| 成绩口径 | 可评满分 + 难度达标线 | **同左，保持一致** | 可比 |
| 申诉 | 无 | 有（复核留痕） | R3 |

---

## 4. 页面清单

### 4.1 管理端（新增 3 页）

| 页 | 路由（建议） | 说明 |
|---|---|---|
| **试卷列表** | `/exam-papers` | 所有试卷：名称/用途/题量/时长/发布状态/已考人数 |
| **试卷编辑器** | `/exam-papers/:id` | 组卷（选题 + 抽题规则 + 时长 + 达标线）→ 发布 |
| **成绩管理** | `/exam-scores` | 按考试/班级/考生查看，明细、复核、导出 |

> 复用/对齐：列表沿用「病例库」那套（`.card` + `.filter-row` + `.table` + 双右固定列 + 表内空态）；
> 编辑器沿用「病例编辑器」外壳（钉顶头部 + 钉顶基本信息 + 标签栏 + 面板）。

### 4.2 考试端（`apps/exam`，新增/改造 3 页）

| 页 | 说明 |
|---|---|
| **登录/校验** | 学号/工号 + 考试码；校验是否在名单、是否已考、是否在考试窗口内 |
| **考试须知** | 考试名称/题量/时长/注意事项/防作弊声明；勾选"我已阅读"才能开始 |
| **作答页** | 考试端主页面（复用训练工作台，去掉伴学与对照）+ 顶部考试状态条 |

---

## 5. 逐页设计

### 5.1 试卷列表（管理端）

```
┌ 试卷管理                                        [ + 新建试卷 ] ┐
│ [搜索试卷名称/编码] [用途▾] [状态▾] [专业▾]        共 N 份      │
├──────────────────────────────────────────────────────────────┤
│ 试卷名称          用途    题量  时长  达标线   发布状态  已考  操作 │
│ 2026住培影像报告月考A 正式考核  3题  60min  按难度  已发布  28/30  … │
└──────────────────────────────────────────────────────────────┘
```

字段：`name` / `purpose`（练习考·正式考核）/ `questionCount` / `durationMin` / `passStrategy` /
`published`（草稿·已发布·已结束）/ `assignedCount` / `submittedCount` / `updatedAt`

### 5.2 试卷编辑器（管理端）

```
┌ 试卷编辑器  ID: PAPER-001  [已发布] v1        [返回][保存草稿][发布] ┐
├ 试卷名称[____] 用途[正式考核▾] 时长[60]分 达标[按难度▾]  ────────────┤
│ [题目与规则] [发布范围] [成绩口径]                                    │
├────────────────────────────────────────────────────────────────────┤
│ 【题目与规则】                                                       │
│ 组题方式  ( )手工选题  (•)按条件抽题                                 │
│ 抽题条件  部位[胸部▾] 模态[CT▾] 难度[U2×1 R1×2] 可评满分≥[80]        │
│           [试抽一次]  ← 抽出来先看，不满意再调                       │
│ 题目顺序  (•)随机  ( )固定                                           │
│ ─────────────────────────────────────────────────────────────────  │
│ 已选题目  ① 胸部CT 病例 23  可评84  U2   [替换][移除][上移][下移]     │
│           ② 颅脑MR 病例 16  可评88  R1   …                          │
│ ⚠ 发布后本卷**冻结题库引用** —— 题库后续改动不影响本卷成绩            │
└────────────────────────────────────────────────────────────────────┘
```

**【发布范围】标签**：按班级 / 按名单（粘贴学号）/ 考试窗口（起-止时间）/ 允许多次考试？默认否。

**【成绩口径】标签**：
- 满分口径：`按本卷可评满分归一化`（推荐，与训练侧一致）/ `原始分` / `折算百分制`
- 达标线：`按难度分层标定`（用样本自身难度）/ 手动指定分数
- 是否计入总评（预留字段）

### 5.3 登录/校验（考试端）

- 输入：学号/工号 + 考试码（或从统一身份进入带 token）
- 校验四件事并在失败时给**明确原因**：不在名单 / 已交卷 / 未到考试时间 / 已过截止
- 成功 → 考试须知

### 5.4 考试须知（考试端）

```
┌ 2026住培影像报告月考A ────────────────────────────────────┐
│ 题量 3 题    时长 60 分钟    开始后连续计时，中途退出不暂停    │
│ 达标线 按难度分层标定                                       │
│ ─────────────────────────────────────────────────────── │
│ 注意事项                                                │
│ 1. 本考试不提供 AI伴学，交卷后也**不提供参考报告**          │
│ 2. 交卷后不可修改；时间到自动交卷                          │
│ 3. 考试期间请勿切屏或离开页面，离开会被记录                │
│ 4. 考试期间如遇断网，请留在本页等待恢复；已作答内容会自动保存 │
│ ☐ 我已阅读并同意                                         │
│                                    [ 开始考试 ]           │
└─────────────────────────────────────────────────────────┘
```

> "开始考试"需勾选 + 是用户手势 → 在这里请求全屏。

### 5.5 作答页（考试端）

```
┌ 2026住培影像报告月考A · 进行中              ⏱ 剩余 42:15   [交卷] ┐
│ 第 1/3 题 · 胸部 · CT · U2        👁 离开 0 次                    │
├───────────────────────────────┬─────────────────────────────────┤
│ PACS 阅片器                    │ 患者临床信息（系统给出·只读）      │
│ 工具条/序列栏/四角叠加/层面条    │ 姓名 年龄 性别 科别 检查时间       │
│ + 测量 + 缩放                  │ 患者病史 ____                    │
│                               ├─────────────────────────────────┤
│                               │ 临床目的与检查方法  0/500         │
│                               │ 影像所见            0/3000        │
│                               │ 诊断意见            0/3000        │
│                               │ 合计 0/6500                      │
├───────────────────────────────┴─────────────────────────────────┤
│ 题号条  ①● ②○ ③○            （● 已作答 / ○ 未作答）              │
└─────────────────────────────────────────────────────────────────┘
```

**与训练工作台的差异（原型必须体现）**：

| 训练工作台有的 | 考试端 |
|---|---|
| 右侧 AI伴学粘性面板 | **去掉** |
| 阅片笔记 | 保留（浮层或折叠），**明确标注"不计入答卷"** |
| 单题、无题号条 | **加题号条**（多题切换、已答/未答状态） |
| 提交后弹成绩报告 | **交卷后进"已交卷"页**，不弹报告（避免在考场里看分数） |
| 顶栏"提交报告" | 顶栏"交卷"（**整卷**交卷）+ 到点自动交卷 |

**关键交互规则**：
- 切题不丢内容（每题独立草稿，本地即时保存 + 每 30s 上报服务端）
- 已答/未答标记在题号条上；交卷前给**未答题清单**确认
- 离开检测：state 条显示次数；超过阈值（建议 3 次）弹提示并**上报异常标记**（不强制交卷）
- 断网：显示离线提示条，内容继续本地保存；恢复后补报

### 5.6 已交卷页（考试端）

```
┌ 已交卷 ──────────────────────────────────────────────┐
│ 你的答卷已提交，正在评阅。                              │
│ 交卷时间 14:32    用时 47 分 12 秒    作答 3 题         │
│                                                      │
│ ⚠ 本次考试不提供参考答案与逐题得分                     │
│   （教练/教师可在成绩管理中查看明细）                   │
└──────────────────────────────────────────────────────┘
```

**设计取舍**：考生侧**不显示分数与点评**。
理由：① 正式考核的分数应由教师发布而非系统即时给出；② 逐要点点评含"缺什么"的信息，
在**同一场考试的其他批次还没考完**时给出去 = 泄题；③ 避免考生在考场内互相通报。
> 若院方要求"考完即看分"，则必须**等本场考试窗口全部结束**才开放，且**只给总分不给明细**。

### 5.7 成绩管理（管理端）

```
┌ 成绩管理 ────────────────────────────────────────────────────┐
│ [考试▾] [班级▾] [状态▾ 全部/评阅中/已评分/评分失败] [搜索] 共 N │
├──────────────────────────────────────────────────────────────┤
│ 考生     考试         总分     可评  达标   状态    交卷时间  操作 │
│ 2021xxx 月考A        72.5/88  88    ✓达标  已评分  14:32   明细 │
├──────────────────────────────────────────────────────────────┤
│ 【明细】逐题 → 逐维度 → 逐条目 → 逐要点（档位/要点分值/点评）      │
│        右侧：答卷原文（可对照，仅教师可见）                        │
│        底部：申诉记录 / 人工改分（填理由，留痕）                   │
└──────────────────────────────────────────────────────────────┘
```

- **教师可见「报告对照」**（学员不可见）—— 这是训练侧那个页签的正确归宿
- **批量操作**：重新批阅（评分失败的）、导出（Excel/CSV）、发布成绩
- **统计**：均分 / 及格率 / **题目难度系数**（按样本：该题平均得分率）

---

## 6. 状态机

```
试卷      draft ──发布──▶ published ──窗口结束──▶ closed
考试会话  notStarted ──开考──▶ inProgress ──交卷/到点──▶ submitted ──批阅──▶ graded
                                                    └──批阅失败──▶ failed ──重试──▶ graded
答卷      (随会话) draft ──▶ submitted ──▶ grading ──▶ graded / failed
申诉      none ──提出──▶ pending ──处理──▶ accepted(改分) / rejected
```

**边界规则**
- `inProgress` 超 `deadline` 未交 → 服务端用**最后一份已上报答卷**结算，标记 `autoSubmitted`
- `failed` 可重试；重试仍失败 → 转人工批阅（保底）
- 已 `graded` 的成绩被人工改分 → 保留原分数与改动理由（不覆盖）

---

## 7. 数据模型（服务端）

| 表 | 关键字段 |
|---|---|
| `exam_paper` | id, name, purpose, duration_min, pass_strategy, order_mode, publish_scope, window_start/end, **snapshot_at**（冻结题库引用的时刻）, status |
| `exam_paper_question` | paper_id, sample_id, seq, **sample_snapshot**（题面快照：影像序列/患者信息/病史，**不含金标准**） |
| `exam_session` | id, paper_id, user_id, started_at, deadline, submitted_at, auto_submitted, leave_count, status |
| `exam_answer` | session_id, sample_id, **segments**（三段原文）, saved_at, submitted_at |
| `exam_score` | session_id, sample_id, raw_total, scoreable_max, pass_line, dims, items(逐要点), score_trace（模型/耗时/口径）, status, error |
| `exam_appeal` | score_id, reason, status, handled_by, handled_at |
| `exam_score_change` | score_id, before, after, reason, by, at |

**三处必须在设计里强调**：
1. `sample_snapshot` **不含 `goldStandard` / `rubric`** —— 服务端出站时剥离（R1）
2. `snapshot_at` 与题目快照 —— 发布即冻结，**题库改动不影响已发布试卷**
3. `score_trace` —— 记录用了哪套模板版本、哪个难度口径、模型与耗时（可复现）

---

## 8. 原型实现建议

**推荐做法：在 `apps/exam` 里做可点击原型，用 mock 试卷数据跑通，不依赖服务端。**

| 原型页 | 数据来源 | 复用 |
|---|---|---|
| 试卷列表 / 试卷编辑器 | 本地 mock 数组（几条试卷 + 一个可抽题的样本池） | 管理端列表/编辑器外壳（但**在 exam app 里建**，避免污染训练端） |
| 考试端登录/须知/作答页 | mock 一份试卷（3 题）+ 现有 `TRAINING_CASES` 作题目 | **`ImageViewer` + `SegmentForm` 原样复用** |
| 已交卷页 | 无数据 | — |
| 成绩管理 | 用 `resolveRubric` + `composeScore` 现算（模拟服务端批阅） | 管理端列表 + `ScoreResultPanel` |

**原型要能演示的四件事**（也是评审的关注点）：
1. **全程没有答案**：题面里看不到标准报告；交卷后也看不到对照
2. **整卷交卷 + 题号条**：多题切换、已答/未答、未答清单确认
3. **时间与离开检测**：倒计时、到点自动交卷、离开次数
4. **成绩明细在教师端**：考生侧无分数，教师侧有完整明细与答卷原文

**原型不要做的**：真实断网、真并发、真鉴权（这些依赖服务端，原型阶段用假状态表达即可）。

---

## 9. 待确认（做原型前需要拍板）

| # | 问题 | 影响 |
|---|---|---|
| 1 | **考生交卷后能否看到分数** | 本设计取"不给"（§5.6）；若院方要求给，需限定"窗口结束后只给总分" |
| 2 | **每人题目顺序是否随机** | 影响试卷模型（等价题组概念）与防抄袭强度 |
| 3 | **允不允许重考** | 影响 `exam_session` 是一次还是多次，以及取哪次成绩 |
| 4 | **达标线用"按难度"还是"手动"** | 与训练侧一致用按难度；若手动则每题一值 |
| 5 | **成绩是否折算百分制** | 各卷可评满分不同（84/88），跨卷比较需要折算口径 |
| 6 | **监考要求**（是否需要摄像头/双机位） | 若需要，属另一模块，不在本设计内 |
