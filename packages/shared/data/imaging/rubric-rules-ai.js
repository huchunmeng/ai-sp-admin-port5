// 逐要点「评分规则」（AI 补写）—— 自动生成，勿手改
//
// 为什么单独一个文件：老样本的评分表是**引入 `rule` 字段之前**抽取的，那一列是空的。
// 这个文件只补 `rule`，不动要点文案 / 可接受表述 / 可评条件，避免重抽把已校对的要点冲掉。
// `rubric.js` 的 resolveRubric 在要点自身没有 rule 时用这里的值。
// 重新生成见 docs/design/影像报告书写训练/05_实现落地/批注响应_20260920_第十一轮.md

export const AI_RULES = {}
